services:
  api:
    build: .
    image: task-management-api
    container_name: task-management-api
    restart: unless-stopped
    env_file: .env
    ports:
      - "3001:3000"
    logging:
      driver: json-file
      options:
        max-size: "10m"
        max-file: "3"
